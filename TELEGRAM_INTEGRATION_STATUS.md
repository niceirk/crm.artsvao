# Статус интеграции Telegram бота

## ✅ Что реализовано (Этап 1 - Базовая интеграция)

### 1. База данных
- ✅ Создана миграция с 4 таблицами:
  - `telegram_accounts` - аккаунты пользователей Telegram
  - `conversations` - диалоги с клиентами
  - `messages` - сообщения в диалогах
  - `notification_templates` - шаблоны уведомлений
- ✅ Добавлены необходимые enum типы
- ✅ Миграция применена к БД

### 2. Backend модули

#### TelegramModule (`backend/src/telegram/`)
- ✅ TelegramService - работа с Telegram Bot API
- ✅ TelegramController - webhook endpoint `/api/telegram/webhook`
- ✅ Обработка команд: `/start`, `/stop_notifications`, `/start_notifications`
- ✅ Поддержка автопривязки через `/start client_<id>`
- ✅ Сохранение входящих сообщений в БД
- ✅ FSM для управления состояниями пользователей

#### MessagesModule (`backend/src/messages/`)
- ✅ MessagesService - бизнес-логика работы с диалогами
- ✅ MessagesController - REST API для менеджеров
- ✅ Эндпоинты:
  - `GET /api/messages/conversations` - список диалогов
  - `GET /api/messages/conversations/:id` - диалог с историей
  - `POST /api/messages/conversations/:id/send` - отправить сообщение
  - `PATCH /api/messages/conversations/:id/status` - обновить статус
  - `POST /api/messages/conversations/:id/link-client` - привязать к клиенту
  - `POST /api/messages/conversations/:id/mark-read` - отметить прочитанным

#### NotificationsModule (`backend/src/notifications/`)
- ✅ NotificationsService - отправка уведомлений клиентам
- ✅ 4 типа уведомлений:
  - Напоминание о занятии (`SCHEDULE_REMINDER`)
  - Подтверждение покупки абонемента (`SUBSCRIPTION_PURCHASED`)
  - Изменение расписания (`SCHEDULE_CHANGED`)
  - Подтверждение оплаты (`PAYMENT_RECEIVED`)
- ✅ Шаблоны уведомлений в БД
- ✅ Интеграция с SubscriptionsModule - автоматическое уведомление при покупке
- ✅ Интеграция с SchedulesModule - автоматическое уведомление при изменении

### 3. Конфигурация
- ✅ Переменные окружения добавлены в `.env.example`:
  - `TELEGRAM_BOT_TOKEN`
  - `TELEGRAM_WEBHOOK_URL`
- ✅ Webhook настроен на `https://crm.artsvao.ru/api/telegram/webhook`

## 🧪 Как протестировать бот

### 1. Убедитесь, что бэкенд запущен
```bash
cd backend
npm run dev
```

### 2. Найдите бота в Telegram
Бот должен быть доступен по имени, которое было указано при создании через @BotFather

### 3. Тесты для проверки

#### Тест 1: Команда /start (новый пользователь)
1. Отправьте `/start` боту
2. Ожидается приветственное сообщение
3. Проверьте в БД: должна создаться запись в `telegram_accounts` и `conversations`

#### Тест 2: Автопривязка к клиенту
1. Получите ID существующего клиента из БД
2. Отправьте боту `/start client_<ID>`
3. Ожидается сообщение с приветствием по имени
4. Проверьте в БД: `telegram_accounts.client_id` должен быть заполнен

#### Тест 3: Отправка текстового сообщения
1. Отправьте любое текстовое сообщение боту
2. Проверьте в БД таблицу `messages`: должно сохраниться новое сообщение
3. Параметры: `direction=INBOUND`, `sender_type=CLIENT`, `is_read_by_manager=false`

#### Тест 4: API - получение списка диалогов
```bash
# Получите токен авторизации
TOKEN="your-jwt-token"

# Запросите список диалогов
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3000/api/messages/conversations"
```

#### Тест 5: API - отправка сообщения клиенту
```bash
# Получите ID диалога из предыдущего теста
CONVERSATION_ID="uuid-диалога"

curl -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"text":"Здравствуйте! Это тестовое сообщение от менеджера."}' \
  "http://localhost:3000/api/messages/conversations/$CONVERSATION_ID/send"
```
Клиент должен получить сообщение в Telegram.

#### Тест 6: Уведомление о покупке абонемента
1. Создайте абонемент для клиента с привязанным Telegram
2. Клиент должен автоматически получить уведомление о покупке

#### Тест 7: Управление уведомлениями
1. Отправьте `/stop_notifications` - уведомления отключены
2. Отправьте `/start_notifications` - уведомления включены
3. Проверьте в БД: `telegram_accounts.is_notifications_enabled`

## ⏳ Что осталось реализовать

### Этап 1 (осталось)
- ⏳ UI компоненты для чата:
  - `ConversationList` - список диалогов
  - `ChatWindow` - окно чата
  - `ChatMessage` - компонент сообщения
  - `ChatInput` - поле ввода сообщения
- ⏳ Страница списка диалогов (`/messages`)
- ⏳ Страница карточки диалога (`/messages/:id`)

### Этап 2 (будущее)
- Расширенная идентификация клиентов по номеру телефона
- Ручная привязка клиентов менеджерами
- История изменения привязки

### Этап 3 (будущее)
- Автоматические напоминания о занятиях (cron job)
- Уведомления об отмене занятий
- Уведомления о долгах/задолженностях
- Персонализированные сообщения

## 📁 Структура файлов

### Backend
```
backend/src/
├── telegram/
│   ├── telegram.module.ts
│   ├── telegram.service.ts
│   ├── telegram.controller.ts
│   ├── dto/
│   └── interfaces/
├── messages/
│   ├── messages.module.ts
│   ├── messages.service.ts
│   ├── messages.controller.ts
│   └── dto/
├── notifications/
│   ├── notifications.module.ts
│   ├── notifications.service.ts
│   └── seed-templates.ts
└── prisma/
    └── migrations/20251122143000_add_telegram_integration/
```

## 🔑 Важные переменные окружения

Добавьте в `backend/.env`:
```env
TELEGRAM_BOT_TOKEN="5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8"
TELEGRAM_WEBHOOK_URL="https://crm.artsvao.ru/api/telegram/webhook"
```

## 📝 Полезные команды

### Пересоздать шаблоны уведомлений
```bash
cd backend
npx ts-node src/notifications/seed-templates.ts
```

### Проверить статус webhook
```bash
curl "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/getWebhookInfo"
```

### Удалить webhook (для отладки с polling)
```bash
curl -X POST "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/deleteWebhook"
```

### Переустановить webhook
```bash
./backend/setup-telegram-webhook.sh
```

## 🎯 Следующие шаги

1. **Протестировать бэкенд** - выполнить все тесты выше
2. **Создать UI компоненты** - реализовать интерфейс для менеджеров
3. **Интеграция с существующим UI** - добавить раздел "Сообщения" в навигацию
4. **Production deployment** - убедиться что HTTPS работает на crm.artsvao.ru

## ⚠️ Важные замечания

- Webhook работает только по HTTPS
- Убедитесь что сервер доступен по адресу `https://crm.artsvao.ru`
- Бот токен уже настроен в webhook, изменять не нужно
- Все уведомления отправляются асинхронно и не блокируют основной поток
