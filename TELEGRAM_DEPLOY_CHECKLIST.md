# Чеклист проверки Telegram бота после деплоя

## 📋 Перед деплоем

### 1. Проверить переменные окружения на продакшен сервере
Убедитесь, что в `.env` файле на продакшен сервере есть:

```bash
# Токен Telegram бота
TELEGRAM_BOT_TOKEN="5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8"

# AWS S3 для хранения фотографий (должны быть заполнены)
AWS_S3_BUCKET_NAME="48054a0c-artsvao"
AWS_S3_REGION="ru-1"
AWS_ACCESS_KEY_ID="7HA0M1ASHRMKI665G5YS"
AWS_SECRET_ACCESS_KEY="xWbEJN4O52DEZEUMCFBjjkpSUbPcSB6r4lm8Gu8q"
AWS_S3_ENDPOINT="https://s3.timeweb.com"
```

### 2. Вебхук уже настроен ✅
Вебхук настроен на продакшен URL:
```
https://crm.artsvao.ru/api/telegram/webhook
```

---

## ✅ После деплоя - проверки

### 1. Проверить, что backend запустился
```bash
curl https://crm.artsvao.ru/api/health
```
Ожидаемый результат:
```json
{"status": "ok", "timestamp": "..."}
```

### 2. Проверить, что эндпоинт Telegram доступен
```bash
curl -X GET https://crm.artsvao.ru/api/telegram/webhook-info
```
Должен вернуть информацию о вебхуке (не 404).

### 3. Проверить статус вебхука через API Telegram
```bash
curl -s "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/getWebhookInfo" | python3 -m json.tool
```

Ожидаемый результат:
```json
{
    "ok": true,
    "result": {
        "url": "https://crm.artsvao.ru/api/telegram/webhook",
        "has_custom_certificate": false,
        "pending_update_count": 0,  // Должно быть 0 или небольшое число
        "max_connections": 40
    }
}
```

**ВАЖНО:** Не должно быть полей `last_error_date` и `last_error_message`!

### 4. Проверить базу данных - миграции применены
```bash
# На продакшен сервере
cd /path/to/backend
npx prisma migrate status
```

Все миграции должны быть применены, включая:
- `20251122143000_add_telegram_integration`
- `20251122162321_add_message_text_index`
- `20251122164314_add_telegram_photo_url`
- `20251122165000_unique_conversation_per_account`
- `20251122172630_add_sender_to_messages`

### 5. Тестовое сообщение боту
1. Откройте бот в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

### 6. Проверить логи backend на ошибки
```bash
# На продакшен сервере
pm2 logs backend --lines 50
# или
journalctl -u backend -n 50 --no-pager
```

Проверьте, что нет ошибок связанных с Telegram.

### 7. Проверить отправку сообщений
1. Отправьте боту текстовое сообщение
2. Отправьте боту фотографию
3. Проверьте, что сообщения появляются в разделе "Сообщения" CRM

### 8. Проверить работу с клиентами
1. Отправьте боту номер телефона
2. Система должна найти клиента или предложить создать нового
3. Проверьте, что связь клиента и Telegram аккаунта работает

---

## 🐛 Возможные проблемы и решения

### Проблема: 404 Not Found при проверке вебхука
**Решение:** Backend не развернут или не перезапущен после деплоя
```bash
# Перезапустить backend
pm2 restart backend
# или
systemctl restart backend
```

### Проблема: Вебхук показывает ошибки в `last_error_message`
**Решение:**
1. Проверьте логи backend
2. Убедитесь, что TELEGRAM_BOT_TOKEN правильный
3. Проверьте, что миграции БД применены
4. Перезапустите вебхук:
```bash
# Удалить вебхук
curl -X POST "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/deleteWebhook"

# Установить заново
curl -X POST "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://crm.artsvao.ru/api/telegram/webhook"}'
```

### Проблема: Фотографии не загружаются
**Решение:** Проверьте настройки AWS S3 в .env файле

### Проблема: База данных не содержит таблицы для Telegram
**Решение:** Применить миграции
```bash
cd /path/to/backend
npx prisma migrate deploy
```

---

## 📝 Команды для быстрой проверки

Сохраните этот скрипт для быстрой проверки:

```bash
#!/bin/bash
echo "=== Проверка Telegram бота ==="

echo -e "\n1. Health check:"
curl -s https://crm.artsvao.ru/api/health | python3 -m json.tool

echo -e "\n2. Telegram webhook info endpoint:"
curl -s https://crm.artsvao.ru/api/telegram/webhook-info | python3 -m json.tool

echo -e "\n3. Telegram API webhook status:"
curl -s "https://api.telegram.org/bot5193485073:AAHnsd-6Bc1FHYsw80QErZFcKYtE9_3RbO8/getWebhookInfo" | python3 -m json.tool

echo -e "\n=== Проверка завершена ==="
```

---

## ✅ Финальная проверка

После выполнения всех шагов:
- [ ] Backend отвечает на health check
- [ ] Эндпоинт /api/telegram/webhook-info доступен
- [ ] Вебхук настроен правильно (нет ошибок в getWebhookInfo)
- [ ] Миграции БД применены
- [ ] Бот отвечает на /start
- [ ] Бот принимает и сохраняет сообщения
- [ ] Фотографии загружаются в S3
- [ ] Сообщения отображаются в CRM

Если все пункты выполнены - Telegram бот готов к работе! 🎉
