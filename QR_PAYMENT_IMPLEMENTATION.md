# Реализация QR-кода оплаты для бюджетного учреждения

**Дата начала:** 19 ноября 2025
**Статус:** В процессе
**Цель:** Реализация генерации QR-кодов для оплаты за кружки по стандарту ГОСТ Р 56042-2014 с отправкой через email

---

## Спецификация

### Тип организации
**Государственное бюджетное учреждение** (оплата за кружки/секции)

### Обязательные поля для бюджетных платежей

1. **Получатель платежа** - наименование организации
2. **ИНН получателя** - 10 или 12 символов
3. **КПП получателя** - 9 символов
4. **Номер счёта получателя** - казначейский счёт / ЕКС (20 символов)
5. **Банк получателя** - наименование
6. **БИК банка** - 9 символов
7. **Корреспондентский счёт** - счёт банка получателя (20 символов)
8. **КБК** (Код бюджетной классификации) - 20 символов
9. **ОКТМО** (Общероссийский классификатор территорий) - 8 или 11 символов
10. **Назначение платежа** - описание
11. **ФИО плательщика** - Фамилия, Имя, Отчество
12. **Сумма платежа** - в рублях

### Формат QR-кода

**Стандарт:** ГОСТ Р 56042-2014
**Кодировка:** ST00012 (UTF-8)
**Формат:** `ST00012|Name=...|PersonalAcc=...|BankName=...|BIC=...|CorrespAcc=...|PayeeINN=...|KPP=...|CBC=...|OKTMO=...|Purpose=...|Sum=...|LastName=...|FirstName=...|MiddleName=...`

---

## Прогресс выполнения

### ✅ Этап 1: Подготовка (Завершено)
- [x] Установка зависимости `qrcode` (v1.5.4)
- [x] Установка типов `@types/qrcode`
- [x] Создание документации по реализации

**Файлы:**
- `package.json` - добавлены зависимости

---

### 🔄 Этап 2: База данных (В процессе)

#### Задачи:
- [ ] Создать миграцию для таблицы `OrganizationDetails`
- [ ] Применить миграцию
- [ ] Создать seed для заполнения реквизитов организации

#### Структура таблицы `OrganizationDetails`:

```prisma
model OrganizationDetails {
  id                String   @id @default(uuid())

  // Основные данные
  organizationName  String   // Полное наименование
  inn               String   // ИНН
  kpp               String   // КПП

  // Банковские реквизиты
  treasuryAccount   String   // Казначейский счет / ЕКС
  bankName          String   // Наименование банка
  bic               String   // БИК
  correspAcc        String   // Корр. счет

  // Бюджетные коды
  defaultKBK        String   // КБК по умолчанию (20 символов)
  defaultOKTMO      String   // ОКТМО (8 или 11 символов)

  isPrimary         Boolean  @default(true)
  createdAt         DateTime @default(now()) @map("created_at")
  updatedAt         DateTime @updatedAt @map("updated_at")

  @@map("organization_details")
}
```

**Файлы для создания:**
- `backend/prisma/schema.prisma` - добавить модель
- `backend/prisma/seed/organization-details.seed.ts` - seed данные

---

### ⏳ Этап 3: QR Generator Service (Ожидает)

#### Задачи:
- [ ] Создать директорию `backend/src/invoices/qr/`
- [ ] Создать `qr-generator.service.ts`
- [ ] Реализовать методы:
  - `generateBuffer(data: string): Promise<Buffer>`
  - `generateDataURL(data: string): Promise<string>`
  - `generateSVG(data: string): Promise<string>`

**Местоположение:** `backend/src/invoices/qr/qr-generator.service.ts`

**Функциональность:**
- Генерация QR-кода в формате PNG (Buffer)
- Генерация QR-кода как Data URL (для HTML)
- Генерация QR-кода в формате SVG
- Настройка уровня коррекции ошибок (L, M, Q, H)
- Настройка размера и отступов

---

### ⏳ Этап 4: Payment Data Builder (Ожидает)

#### Задачи:
- [ ] Создать `qr-payment-data.builder.ts`
- [ ] Определить интерфейс `BudgetPaymentData`
- [ ] Реализовать метод `buildBudgetPaymentString()`
- [ ] Реализовать метод `validate()`
- [ ] Реализовать метод `escape()`

**Местоположение:** `backend/src/invoices/qr/qr-payment-data.builder.ts`

**Интерфейс:**
```typescript
interface BudgetPaymentData {
  // Получатель
  Name: string;
  PayeeINN: string;
  KPP: string;
  PersonalAcc: string;
  BankName: string;
  BIC: string;
  CorrespAcc: string;

  // Бюджетные поля
  CBC: string;        // КБК
  OKTMO: string;      // ОКТМО

  // Платеж
  Purpose: string;
  Sum: number;

  // Плательщик
  LastName: string;
  FirstName: string;
  MiddleName?: string;

  // Дополнительные
  PayerINN?: string;
  DrawerStatus?: string;  // '0' для физлиц
  DocNo?: string;
  DocDate?: string;
}
```

---

### ⏳ Этап 5: Интеграция с InvoicesService (Ожидает)

#### Задачи:
- [ ] Добавить инъекцию `QRGeneratorService` в конструктор
- [ ] Реализовать метод `generateQRCode(invoiceId: string)`
- [ ] Реализовать метод `getOrganizationDetails()`
- [ ] Реализовать метод `getKBKForService(invoice)`
- [ ] Обновить метод `create()` для автоматической отправки QR при создании счета

**Местоположение:** `backend/src/invoices/invoices.service.ts`

**Новые методы:**
```typescript
async generateQRCode(invoiceId: string): Promise<{
  buffer: Buffer;
  dataUrl: string;
}>

async getOrganizationDetails(): Promise<OrganizationDetails>

async getKBKForService(invoice: Invoice): Promise<{
  kbk: string;
  oktmo: string;
}>
```

---

### ⏳ Этап 6: API Endpoints (Ожидает)

#### Задачи:
- [ ] Добавить endpoint `GET /invoices/:id/qr`
- [ ] Добавить endpoint `GET /invoices/:id/qr-data-url`
- [ ] Добавить endpoint `POST /invoices/:id/send-qr-email`

**Местоположение:** `backend/src/invoices/invoices.controller.ts`

**Endpoints:**
```typescript
@Get(':id/qr')
async getQRCode(@Param('id') id: string, @Res() res: Response): Promise<void>

@Get(':id/qr-data-url')
async getQRDataURL(@Param('id') id: string): Promise<{ dataUrl: string }>

@Post(':id/send-qr-email')
async sendQREmail(@Param('id') id: string): Promise<{ success: boolean }>
```

---

### ⏳ Этап 7: React Email Template (Ожидает)

#### Задачи:
- [ ] Создать `InvoiceQREmail.tsx`
- [ ] Реализовать компонент с QR-кодом
- [ ] Добавить инструкции по оплате
- [ ] Стилизовать в едином стиле ArtsVAO

**Местоположение:** `backend/src/email/templates/InvoiceQREmail.tsx`

**Содержание письма:**
- Шапка с логотипом/названием ArtsVAO
- Информация о счете (номер, сумма, срок оплаты)
- QR-код (встроенный через Data URL)
- Пошаговая инструкция по оплате через мобильный банк
- Примечания о бюджетных платежах
- Контактная информация

---

### ⏳ Этап 8: Email Service Integration (Ожидает)

#### Задачи:
- [ ] Добавить метод `sendInvoiceWithQR()` в EmailService
- [ ] Интегрировать с React Email шаблоном
- [ ] Добавить логирование в EmailLog
- [ ] Обработка ошибок

**Местоположение:** `backend/src/email/email.service.ts`

**Новый метод:**
```typescript
async sendInvoiceWithQR(
  email: string,
  invoiceData: {
    invoiceNumber: string;
    clientName: string;
    totalAmount: number;
    dueDate?: Date;
  },
  qrDataUrl: string,
  qrBuffer?: Buffer,
): Promise<void>
```

---

### ⏳ Этап 9: Module Configuration (Ожидает)

#### Задачи:
- [ ] Добавить `QRGeneratorService` в providers
- [ ] Добавить импорт `EmailModule`
- [ ] Обновить exports если нужно

**Местоположение:** `backend/src/invoices/invoices.module.ts`

---

### ⏳ Этап 10: Seed & Testing (Ожидает)

#### Задачи:
- [ ] Создать seed для реквизитов организации
- [ ] Запустить seed
- [ ] Протестировать генерацию QR
- [ ] Протестировать отправку email
- [ ] Проверить QR в мобильных банках (Сбербанк, Тинькофф, ВТБ)
- [ ] Unit-тесты для QRPaymentDataBuilder
- [ ] E2E тесты для API endpoints

---

## Необходимые данные для заполнения

### Реквизиты организации (предоставить)

```typescript
{
  organizationName: '?',     // Полное наименование
  inn: '?',                  // ИНН (10 или 12 цифр)
  kpp: '?',                  // КПП (9 цифр)
  treasuryAccount: '?',      // Казначейский счет (20 цифр)
  bankName: '?',             // Название банка
  bic: '?',                  // БИК (9 цифр)
  correspAcc: '?',           // Корр. счет (20 цифр)
  defaultKBK: '?',           // КБК (20 цифр)
  defaultOKTMO: '?',         // ОКТМО (8 или 11 цифр)
}
```

---

## Структура файлов

### Новые файлы:
```
backend/
├── src/
│   ├── invoices/
│   │   ├── qr/
│   │   │   ├── qr-generator.service.ts       [НОВОЕ]
│   │   │   └── qr-payment-data.builder.ts    [НОВОЕ]
│   │   ├── invoices.controller.ts            [ИЗМЕНЕНИЯ]
│   │   ├── invoices.service.ts               [ИЗМЕНЕНИЯ]
│   │   └── invoices.module.ts                [ИЗМЕНЕНИЯ]
│   └── email/
│       ├── templates/
│       │   └── InvoiceQREmail.tsx            [НОВОЕ]
│       └── email.service.ts                  [ИЗМЕНЕНИЯ]
├── prisma/
│   ├── schema.prisma                         [ИЗМЕНЕНИЯ]
│   └── seed/
│       └── organization-details.seed.ts      [НОВОЕ]
└── package.json                              [ИЗМЕНЕНИЯ]
```

---

## API Endpoints

После завершения будут доступны:

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/invoices/:id/qr` | Скачать QR-код как PNG |
| GET | `/api/invoices/:id/qr-data-url` | Получить QR как Data URL |
| POST | `/api/invoices/:id/send-qr-email` | Отправить QR на email клиента |

---

## Технологический стек

- **QR Generator:** `qrcode` v1.5.4
- **Email:** `@nestjs-modules/mailer` + `nodemailer`
- **Email Templates:** `@react-email/components`
- **Database:** PostgreSQL + Prisma ORM
- **Framework:** NestJS

---

## Следующие шаги

1. ✅ Установить зависимости
2. 🔄 Создать миграцию БД для OrganizationDetails
3. ⏳ Создать QR Generator Service
4. ⏳ Создать Payment Data Builder
5. ⏳ Интегрировать с InvoicesService
6. ⏳ Создать API endpoints
7. ⏳ Создать Email шаблон
8. ⏳ Интегрировать с EmailService
9. ⏳ Настроить реквизиты организации
10. ⏳ Тестирование

---

## Примечания

- Для бюджетных платежей обязательны поля КБК и ОКТМО
- Разные услуги могут иметь разные КБК (нужно уточнить у бухгалтерии)
- Статус плательщика для физлиц = '0'
- Назначение платежа должно содержать: услугу, период, ФИО

---

**Последнее обновление:** 19 ноября 2025, 15:30
**Следующая задача:** Создание миграции БД для OrganizationDetails
