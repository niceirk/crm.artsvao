# Модуль 09: Детальные страницы студии и группы

## Обзор

Модуль обеспечивает детальную информацию о студиях и группах через две страницы:
- **Страница студии** (`/studios/[id]`) - обзор студии, списки групп и типов абонементов
- **Страница группы** (`/groups/[id]`) - детальная информация о группе, участники, расписание и планировщик месяца

## Архитектура

### Модель данных

#### Расширение модели Group

Добавлены новые поля для управления расписанием:

```prisma
model Group {
  // ... существующие поля
  duration          Int?   // Длительность занятия в минутах
  weeklySchedule    Json?  @map("weekly_schedule") // Паттерн расписания
  // ...
}
```

**weeklySchedule** - JSON массив с недельным паттерном:
```typescript
interface WeeklyScheduleItem {
  day: 'MON' | 'TUE' | 'WED' | 'THU' | 'FRI' | 'SAT' | 'SUN';
  startTime: string; // Формат "HH:mm", например "18:00"
}

// Пример weeklySchedule
[
  { day: 'MON', startTime: '18:00' },
  { day: 'WED', startTime: '18:00' },
  { day: 'FRI', startTime: '18:00' }
]
```

**duration** - длительность занятия в минутах (например, 90 для полуторачасового занятия).
Время окончания рассчитывается автоматически: `endTime = startTime + duration`.

#### Хранение участников группы

Участники группы определяются через **активные абонементы** (модель Subscription):
- Клиент считается участником группы, если у него есть активный абонемент (`status = 'ACTIVE'`)
- При добавлении участника создается Subscription
- При удалении участника Subscription переводится в статус `CANCELLED`

---

## Backend API

### Studios API

#### 1. GET /api/studios/:id/groups
Получить список всех групп студии.

**Response:**
```json
[
  {
    "id": "uuid",
    "name": "Танцы для детей",
    "teacherId": "uuid",
    "roomId": "uuid",
    "maxParticipants": 15,
    "duration": 90,
    "weeklySchedule": [
      { "day": "MON", "startTime": "18:00" },
      { "day": "WED", "startTime": "18:00" }
    ],
    "teacher": {
      "id": "uuid",
      "firstName": "Иван",
      "lastName": "Иванов"
    },
    "room": {
      "id": "uuid",
      "name": "Зал 1",
      "number": "101"
    },
    "_count": {
      "subscriptions": 12,
      "schedules": 48
    }
  }
]
```

#### 2. GET /api/studios/:id/subscription-types
Получить все типы абонементов для групп студии.

**Response:**
```json
[
  {
    "id": "uuid",
    "name": "Безлимитный месяц",
    "description": "Неограниченное посещение в течение месяца",
    "type": "UNLIMITED",
    "price": 5000,
    "isActive": true,
    "group": {
      "id": "uuid",
      "name": "Танцы для детей"
    }
  }
]
```

#### 3. GET /api/studios/:id/stats
Получить статистику студии.

**Response:**
```json
{
  "groupsCount": 5,
  "activeSubscriptionsCount": 45,
  "participantsCount": 38
}
```

### Groups API

#### 1. GET /api/groups/:id/members
Получить список участников группы (через активные абонементы).

**Response:**
```json
[
  {
    "id": "subscription-uuid",
    "clientId": "client-uuid",
    "validMonth": "2024-12",
    "startDate": "2024-12-01",
    "endDate": "2024-12-31",
    "remainingVisits": 8,
    "status": "ACTIVE",
    "client": {
      "id": "uuid",
      "firstName": "Мария",
      "lastName": "Петрова",
      "middleName": "Ивановна",
      "phone": "+79001234567",
      "email": "maria@example.com",
      "photoUrl": null
    },
    "subscriptionType": {
      "id": "uuid",
      "name": "8 занятий",
      "type": "SINGLE_VISIT"
    }
  }
]
```

#### 2. POST /api/groups/:id/members
Добавить участника в группу (создать абонемент).

**Request:**
```json
{
  "clientId": "uuid",
  "subscriptionTypeId": "uuid",
  "purchaseDate": "2024-12-01",
  "startDate": "2024-12-01",
  "endDate": "2024-12-31",
  "validMonth": "2024-12",
  "originalPrice": 5000,
  "paidPrice": 5000,
  "remainingVisits": 8,
  "purchasedMonths": 1
}
```

#### 3. DELETE /api/groups/:id/members/:clientId
Удалить участника из группы (отменить абонемент).

**Response:**
```json
{
  "id": "subscription-uuid",
  "status": "CANCELLED"
}
```

#### 4. GET /api/groups/:id/schedule/monthly?year=2024&month=12
Получить расписание группы на месяц.

**Query Parameters:**
- `year` (number) - год
- `month` (number) - месяц (1-12)

**Response:**
```json
[
  {
    "id": "uuid",
    "date": "2024-12-02",
    "startTime": "18:00:00",
    "endTime": "19:30:00",
    "status": "PLANNED",
    "teacher": {
      "id": "uuid",
      "firstName": "Иван",
      "lastName": "Иванов"
    },
    "room": {
      "id": "uuid",
      "name": "Зал 1",
      "number": "101"
    },
    "_count": {
      "attendances": 12
    }
  }
]
```

#### 5. PATCH /api/groups/:id/weekly-schedule
Обновить недельный паттерн расписания группы.

**Request:**
```json
{
  "weeklySchedule": [
    { "day": "MON", "startTime": "18:00" },
    { "day": "WED", "startTime": "18:00" },
    { "day": "FRI", "startTime": "18:00" }
  ]
}
```

---

## Frontend (план реализации)

### Страница студии: `/studios/[id]`

**Компоненты:**
- `studio-detail-page.tsx` - основная страница
- `studio-info-card.tsx` - карточка с информацией о студии
- `studio-groups-table.tsx` - таблица групп студии
- `studio-subscription-types.tsx` - список типов абонементов
- `studio-stats.tsx` - статистика (карточки с числами)
- `edit-studio-dialog.tsx` - диалог редактирования студии

**Структура страницы:**
1. **Шапка:**
   - Название, описание, фото студии
   - Статус (ACTIVE/INACTIVE)
   - Кнопка "Редактировать"

2. **Статистика (карточки):**
   - Количество групп
   - Всего участников
   - Активных абонементов

3. **Таблица групп:**
   - Название (клик → `/groups/[id]`)
   - Преподаватель
   - Помещение
   - Участников / Макс
   - Паттерн расписания (Пн, Ср, Пт - 18:00)
   - Статус

4. **Список типов абонементов:**
   - Название тарифа
   - Тип (месячный/разовый)
   - Цена
   - Для какой группы

### Страница группы: `/groups/[id]`

**Компоненты:**
- `group-detail-page.tsx` - основная страница
- `group-info-card.tsx` - информация о группе
- `weekly-schedule-editor.tsx` - редактор недельного паттерна
- `group-members-table.tsx` - таблица участников
- `add-member-dialog.tsx` - диалог добавления участника
- `month-planner.tsx` - планировщик месяца
- `group-monthly-calendar.tsx` - календарь занятий группы
- `edit-group-dialog.tsx` - диалог редактирования группы

**Структура страницы:**
1. **Хлебные крошки:**
   `Студии → [Название студии] → [Название группы]`

2. **Шапка группы:**
   - Название, описание
   - Преподаватель, помещение
   - Макс. участников, возраст, цена занятия
   - **Длительность занятия** (новое поле)
   - Кнопка "Редактировать"

3. **Недельный паттерн расписания:**
   - Чекбоксы дней недели: ☑ Пн ☑ Ср ☐ Чт ☑ Пт
   - Для каждого выбранного дня: поле времени начала
   - Отображение времени окончания (начало + duration)
   - Кнопка "Сохранить паттерн"

4. **Список участников (таблица):**
   - ФИО клиента (клик → `/clients/[id]`)
   - Статус абонемента
   - Действует до (validMonth)
   - Осталось посещений
   - Кнопка "Добавить участника"
   - Кнопки удаления

5. **Планировщик месяца:**
   - Выбор месяца (date picker)
   - Кнопка "Запланировать месяц"
   - Форма с предзаполненными данными из `weeklySchedule`
   - Предпросмотр создаваемых занятий
   - Проверка конфликтов
   - Календарь с метками
   - Кнопка "Создать расписание"

6. **Расписание на месяц (календарь):**
   - Месячный календарь с занятиями группы
   - Клик на занятие → редактирование
   - Фильтр по месяцу

---

## Компонент: WeeklyScheduleEditor

Редактор недельного паттерна расписания.

### Props
```typescript
interface WeeklyScheduleEditorProps {
  value: WeeklyScheduleItem[];
  duration: number; // Длительность занятия в минутах
  onChange: (schedule: WeeklyScheduleItem[]) => void;
  onSave?: () => Promise<void>;
}
```

### UI
- 7 строк для дней недели (Пн - Вс)
- Каждая строка:
  - Чекбокс включения/выключения дня
  - Поле времени начала (TimeInput)
  - Автоматическое отображение времени окончания (startTime + duration)
- Кнопка "Сохранить паттерн"

### Логика
```typescript
const calculateEndTime = (startTime: string, duration: number): string => {
  const [hours, minutes] = startTime.split(':').map(Number);
  const totalMinutes = hours * 60 + minutes + duration;
  const endHours = Math.floor(totalMinutes / 60) % 24;
  const endMinutes = totalMinutes % 60;
  return `${String(endHours).padStart(2, '0')}:${String(endMinutes).padStart(2, '0')}`;
};
```

---

## Компонент: MonthPlanner

Встроенный планировщик расписания на месяц.

### Props
```typescript
interface MonthPlannerProps {
  groupId: string;
  weeklySchedule: WeeklyScheduleItem[];
  duration: number;
  teacherId: string;
  roomId: string;
}
```

### Процесс планирования

1. **Выбор месяца:**
   - Месяц/год через DatePicker

2. **Автозаполнение формы:**
   ```typescript
   {
     groupId: groupId, // уже выбрана
     daysOfWeek: weeklySchedule.map(s => s.day), // из weeklySchedule
     startTimes: weeklySchedule, // время для каждого дня
     startDate: '2024-12-01', // начало месяца
     endDate: '2024-12-31', // конец месяца
     duration: duration, // из Group.duration
     teacherId: teacherId, // из Group.teacherId
     roomId: roomId // из Group.roomId
   }
   ```

3. **Предпросмотр:**
   - API запрос `POST /api/schedules/preview`
   - Получение списка дат с проверкой конфликтов
   - Календарь с метками:
     - ✅ Зеленые - создадутся
     - ⚠️ Желтые - конфликты (пропустятся)

4. **Создание:**
   - Кнопка "Создать расписание"
   - API запрос `POST /api/schedules/recurring`
   - Автозапись участников группы (из активных Subscription)

### Логика автозаписи участников

При создании расписания через планировщик автоматически создаются Attendance для участников:

```typescript
// Критерии для автозаписи:
- Клиент имеет активный абонемент (Subscription.status = 'ACTIVE')
- Абонемент для этой группы (Subscription.groupId = Schedule.groupId)
- validMonth абонемента совпадает с месяцем занятия
- Для SINGLE_VISIT: remainingVisits > 0

// Создание Attendance:
{
  scheduleId: schedule.id,
  clientId: client.id,
  status: 'PRESENT', // по умолчанию
  subscriptionDeducted: false // списание только при отметке
}
```

---

## Навигация

### Структура маршрутов

```
/admin/studios → Список студий (таблица)
  ↓ клик на строку
/studios/[id] → Детальная страница студии
  ↓ клик на группу
/groups/[id] → Детальная страница группы
```

### Breadcrumbs (хлебные крошки)

На странице группы:
```
Студии → [Название студии] → [Название группы]
```

Клик на "Студии" → `/admin/studios`
Клик на "[Название студии]" → `/studios/[id]`

---

## Примеры использования

### Пример 1: Планирование месяца для группы "Танцы для детей"

1. Переходим на `/groups/abc-123`
2. Видим недельный паттерн:
   - ☑ Пн 18:00 → 19:30 (90 мин)
   - ☑ Ср 18:00 → 19:30
   - ☑ Пт 18:00 → 19:30

3. В разделе "Планировщик месяца":
   - Выбираем "Декабрь 2024"
   - Нажимаем "Запланировать месяц"

4. Система автоматически заполняет форму:
   - Дни недели: Пн, Ср, Пт
   - Время: 18:00
   - Длительность: 90 мин
   - Период: 01.12.2024 - 31.12.2024
   - Преподаватель: (из группы)
   - Помещение: (из группы)

5. Показывается предпросмотр:
   - 02.12 (Пн) ✅
   - 04.12 (Ср) ⚠️ Конфликт (помещение занято)
   - 06.12 (Пт) ✅
   - ... всего 13 занятий, 12 создадутся

6. Нажимаем "Создать расписание"
7. Система создает 12 занятий Schedule
8. Автоматически записывает всех участников группы (12 человек с активными абонементами)

### Пример 2: Добавление нового участника

1. На `/groups/abc-123` в разделе "Участники"
2. Нажимаем "Добавить участника"
3. В диалоге:
   - Выбираем клиента "Мария Петрова"
   - Выбираем тип абонемента "8 занятий"
   - Указываем период: декабрь 2024
   - Цена: 4000 руб.
   - Осталось посещений: 8

4. Система создает Subscription со статусом ACTIVE
5. Мария Петрова появляется в списке участников
6. При следующем планировании месяца она будет автоматически записана на занятия

---

## Интеграция с существующими модулями

### Модуль 02: SCHEDULE_MODULE

Обновления:
- Schedule теперь может использовать `Group.duration` для автоматического расчета времени окончания
- Добавлена поддержка `weeklySchedule` при создании повторяющихся занятий

### Модуль 08: SCHEDULE_PLANNER_MODULE

Обновления:
- Планировщик может использовать `weeklySchedule` группы для автозаполнения формы
- При создании расписания для группы используется `Group.duration`
- Интеграция с детальной страницей группы

---

## Технические детали

### Миграция БД

Файл: `20251115200000_add_group_duration_and_weekly_schedule/migration.sql`
```sql
ALTER TABLE "groups" ADD COLUMN "duration" INTEGER,
ADD COLUMN "weekly_schedule" JSONB;
```

### TypeScript типы

```typescript
// frontend/lib/types/group.ts
export interface WeeklyScheduleItem {
  day: 'MON' | 'TUE' | 'WED' | 'THU' | 'FRI' | 'SAT' | 'SUN';
  startTime: string; // "HH:mm"
}

export interface Group {
  id: string;
  name: string;
  studioId: string;
  teacherId: string;
  roomId?: string;
  maxParticipants: number;
  singleSessionPrice: number;
  ageMin?: number;
  ageMax?: number;
  duration?: number; // минуты
  weeklySchedule?: WeeklyScheduleItem[];
  status: 'ACTIVE' | 'INACTIVE';
  // ...
}
```

### API клиенты

```typescript
// frontend/lib/api/studios.ts
export const getStudioGroups = async (studioId: string) => {
  return api.get(`/studios/${studioId}/groups`);
};

export const getStudioStats = async (studioId: string) => {
  return api.get(`/studios/${studioId}/stats`);
};

// frontend/lib/api/groups.ts
export const getGroupMembers = async (groupId: string) => {
  return api.get(`/groups/${groupId}/members`);
};

export const addGroupMember = async (groupId: string, data: AddMemberDto) => {
  return api.post(`/groups/${groupId}/members`, data);
};

export const updateWeeklySchedule = async (
  groupId: string,
  weeklySchedule: WeeklyScheduleItem[]
) => {
  return api.patch(`/groups/${groupId}/weekly-schedule`, { weeklySchedule });
};
```

---

## Статус реализации

### ✅ Готово (Backend)
- [x] Схема БД обновлена
- [x] Миграция создана
- [x] DTO обновлены
- [x] Studios API endpoints
- [x] Groups API endpoints

### 🚧 В разработке (Frontend)
- [ ] TypeScript типы
- [ ] API клиенты
- [ ] Страница студии `/studios/[id]`
- [ ] Страница группы `/groups/[id]`
- [ ] WeeklyScheduleEditor компонент
- [ ] MonthPlanner компонент
- [ ] Навигация и breadcrumbs

### 📋 Планируется
- [ ] Тесты API endpoints
- [ ] E2E тесты страниц
- [ ] Оптимизация запросов БД
- [ ] Кэширование данных

---

## Следующие шаги

1. Применить миграцию БД: `npx prisma migrate deploy`
2. Сгенерировать Prisma клиент: `npx prisma generate`
3. Создать TypeScript типы для frontend
4. Реализовать API клиенты
5. Создать компоненты страниц
6. Добавить навигацию
7. Протестировать функционал
8. Развернуть на production

---

**Версия:** 1.0
**Дата:** 2024-11-15
**Автор:** Claude Code
