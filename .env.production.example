# Database Configuration
DB_USER=artsvao_user
DB_PASSWORD=your_secure_password_here
DB_NAME=artsvao_db
DB_HOST=postgres
DB_PORT=5432

# Database URL for Prisma (PostgreSQL connection string)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public

# JWT Configuration
# ВАЖНО: Сгенерируйте криптографически стойкие секреты!
# Команда для генерации: openssl rand -base64 64
# Минимальная длина секрета: 256 бит (32 байта base64)
# Никогда не используйте значения по умолчанию в production!
JWT_SECRET=GENERATE_WITH_openssl_rand_-base64_64
JWT_EXPIRATION=12h

# Frontend API URL
NEXT_PUBLIC_API_URL=https://crm.artsvao.ru/api

# Node Environment
NODE_ENV=production

# Email Configuration (SMTP) - REQUIRED
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=notifications@example.com
SMTP_PASSWORD=your_smtp_password
EMAIL_FROM=Your Company <notifications@example.com>
FRONTEND_URL=https://your-domain.com

# Pyrus CRM Integration (OPTIONAL)
PYRUS_LOGIN=your-email@example.com
PYRUS_SECURITY_KEY=your_pyrus_security_key_here
PYRUS_FORM_ID=your_form_id

# YooKassa Payment Integration (OPTIONAL)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Telegram Bot Configuration (REQUIRED for messenger)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
